get:
  handler: services/users/get.main
  environment:
    USER_POOL_ID: ${self:custom.userPoolId}
  events:
    - http:
        path: user/{id}
        method: get
        cors: false
        authorizer: aws_iam
        private: true
