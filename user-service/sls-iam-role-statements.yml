- Effect: Allow
  Action:
    - cognito-idp:AdminGetUser
  Resource:
    - arn:aws:cognito-idp:*:*:userpool/${ssm:UserPoolId}
- Effect: Allow
  Action:
    - ssm:GetParameter
  Resource: arn:aws:ssm:#{AWS::Region}:#{AWS::AccountId}:parameter/UserPoolId
- Effect: Allow
  Action:
    - xray:PutTraceSegments
    - xray:PutTelemetryRecords
  Resource:
    - '*'
- Effect: Allow
  Action:
    - cognito-idp:ListUsers
  Resource:
    - arn:aws:cognito-idp:*:*:userpool/${ssm:UserPoolId}
