'use strict'

async function create({ emailAddress, userId, listId}) {

  
  //Call to cognito to check if email is already registered (via user service)

    
  //if email registered, send email via email service

  
   



}

module.exports = {
  create
}
