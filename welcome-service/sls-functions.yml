handleNewChecklist:
  handler: services/welcome/new-checklist-handler
  environment:
    EMAIL_QUEUE_NAME: ${self:custom.emailQueueName}
  events:
    - cloudwatchEvent:
        event:
          source:
            - 'sliclists.checklist'
          detail-type:
            - 'LIST_CREATED_EVENT'
